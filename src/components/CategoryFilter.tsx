// CategoryFilter.tsx
// Global: Dropdown or button group that lets the user filter challenges by category.
// Categories are: "Food", "Campus", "Social", "Academic"
// Should pass the selected category up to parent using a callback like onFilter(category).
// Component Author:

export default function CategoryFilter() {
    return <></>;
}
export default function CategoryFilter() {
    return <></>;
}