// SpinButton.tsx
// Global: Button that randomly selects a challenge from the list and displays it.
// When clicked, it should pick a random challenge from the challenges array
// and pass it up to the parent (App.tsx) using a callback prop like onSpin(challenge).
// Component Author:

export default function SpinButton() {
    return <></>;
}